spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/rfid?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true

    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  messages:
    basename: i18n.login
  mvc:
    date-format: yyyy-MM-dd
  thymeleaf:
    cache: false

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml

server:
  port: 8081


